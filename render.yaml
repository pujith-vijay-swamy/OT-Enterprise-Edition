services:
  - type: web
    name: repotrace-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python engine/repotrace/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: GEMINI_API_KEY
        sync: false
